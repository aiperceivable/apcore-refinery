# Refined by apcore-refinery (manual run, without skill)
# Original: Auto-generated by flask-apcore scanner (2026-03-15)
# Refined: 2026-03-17

bindings:
- module_id: delete_task.delete
  target: app:delete_task
  description: Permanently delete a task by its ID, removing it from the task list.
  documentation: >
    Permanently removes a task from the in-memory task store by its integer ID.
    Returns {"deleted": true} on success. Returns a 404 error with
    {"error": "not found"} if no task with the given ID exists. This operation
    is irreversible — the deleted task cannot be recovered. The ID is not reused
    for future tasks.
  tags:
    - tasks
    - delete
    - crud
  version: 1.0.0
  annotations:
    readonly: false
    destructive: true
    idempotent: false
    requires_approval: true
    open_world: false
    streaming: false
    cacheable: false
    cache_ttl: 0
    cache_key_fields: null
    paginated: false
    pagination_style: cursor
  metadata:
    source: native
    x-when-to-use: >
      Use this when the user explicitly asks to remove or delete a specific task.
    x-when-not-to-use: >
      Do not use this to mark a task as completed — use update_task.put with
      done=true instead. Do not use without confirming the task ID exists first
      via get_task.get or list_tasks.get.
    x-common-mistakes: >
      Deleting a task without first confirming with the user, since this action
      is permanent and cannot be undone.
  examples:
    - title: Delete a task by ID
      inputs:
        task_id: 1
      output:
        deleted: true
  input_schema:
    type: object
    properties:
      task_id:
        type: integer
        description: >
          The unique integer ID of the task to delete. Must correspond to an
          existing task; a 404 error is returned if the task does not exist.
    required:
    - task_id
  output_schema:
    type: object
    properties:
      deleted:
        type: boolean
        description: >
          True if the task was successfully deleted.
