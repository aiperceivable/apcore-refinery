# Refined by apcore-refinery (manual run, without skill)
# Original: Auto-generated by flask-apcore scanner (2026-03-15)
# Refined: 2026-03-17

bindings:
- module_id: create_task.post
  target: app:create_task
  description: Create a new task with a title, optional description, and completion status.
  documentation: >
    Creates a new task in the in-memory task store and returns the created task
    with its auto-assigned ID. The title is required; description defaults to an
    empty string and done defaults to false. Each new task receives a
    monotonically increasing integer ID. This is a write operation with no
    external side effects beyond mutating the in-memory store. Returns the full
    Task object including the generated ID.
  tags:
    - tasks
    - create
    - crud
  version: 1.0.0
  annotations:
    readonly: false
    destructive: false
    idempotent: false
    requires_approval: false
    open_world: false
    streaming: false
    cacheable: false
    cache_ttl: 0
    cache_key_fields: null
    paginated: false
    pagination_style: cursor
  metadata:
    source: native
    x-when-to-use: >
      Use this when the user wants to add a new task to the task list.
    x-when-not-to-use: >
      Do not use this to update an existing task — use update_task.put instead.
    x-common-mistakes: >
      Forgetting that title is required; passing an integer ID (IDs are
      auto-assigned and must not be provided as input).
  examples:
    - title: Create a simple task
      inputs:
        title: Buy groceries
      output:
        id: 3
        title: Buy groceries
        description: ""
        done: false
    - title: Create a task with description and done status
      inputs:
        title: Write quarterly report
        description: Cover Q1 sales figures and projections
        done: false
      output:
        id: 4
        title: Write quarterly report
        description: Cover Q1 sales figures and projections
        done: false
  input_schema:
    type: object
    properties:
      title:
        title: Title
        type: string
        description: >
          The name of the task to create. This is the primary identifier shown
          in task listings.
      description:
        default: ''
        title: Description
        type: string
        description: >
          Optional free-text details about the task. Defaults to an empty string
          if not provided.
      done:
        default: false
        title: Done
        type: boolean
        description: >
          Whether the task should be marked as completed upon creation. Defaults
          to false.
    required:
    - title
  output_schema:
    properties:
      id:
        title: Id
        type: integer
        description: The auto-assigned unique identifier for the created task.
      title:
        title: Title
        type: string
        description: The title of the created task.
      description:
        title: Description
        type: string
        description: The description of the created task.
      done:
        title: Done
        type: boolean
        description: Whether the created task is marked as completed.
    required:
    - id
    - title
    - description
    - done
    title: Task
    type: object
