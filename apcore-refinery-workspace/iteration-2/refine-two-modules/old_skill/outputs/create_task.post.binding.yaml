# Refined by apcore-refinery
# Original: Auto-generated by flask-apcore scanner (2026-03-15)

bindings:
- module_id: create_task.post
  target: app:create_task
  description: Create a new task with a title, optional description, and completion status
  documentation: |
    Creates a new task in the in-memory task store and returns the complete task
    object including its auto-assigned ID. The title field is required; description
    defaults to an empty string and done defaults to false. Each task receives a
    unique sequential integer ID. Returns the created task as a Task object with
    all fields populated. No authentication is required.
  tags:
  - tasks
  - create
  version: 1.0.0
  annotations:
    readonly: false
    destructive: false
    idempotent: false
    requires_approval: false
    open_world: false
    streaming: false
    cacheable: false
    cache_ttl: 0
    cache_key_fields: null
    paginated: false
    pagination_style: cursor
  metadata:
    source: native
    x-when-to-use: Use this when the user wants to add a new task to the task list.
    x-when-not-to-use: Do not use this to modify an existing task; use update_task.put instead.
    x-common-mistakes: Omitting the required title field or passing a task_id (IDs are auto-assigned).
  input_schema:
    type: object
    properties:
      title:
        title: Title
        type: string
        description: The name or summary of the task to create (required).
      description:
        default: ''
        title: Description
        type: string
        description: An optional longer explanation or details about the task.
      done:
        default: false
        title: Done
        type: boolean
        description: Whether the task is already completed at creation time. Defaults to false.
    required:
    - title
  output_schema:
    properties:
      id:
        title: Id
        type: integer
      title:
        title: Title
        type: string
      description:
        title: Description
        type: string
      done:
        title: Done
        type: boolean
    required:
    - id
    - title
    - description
    - done
    title: Task
    type: object
  examples:
  - title: Create a simple task
    inputs:
      title: Buy groceries
    output:
      id: 3
      title: Buy groceries
      description: ''
      done: false
  - title: Create a task with description and pre-marked done
    inputs:
      title: Write project proposal
      description: Draft the Q3 project proposal for the engineering team
      done: true
    output:
      id: 4
      title: Write project proposal
      description: Draft the Q3 project proposal for the engineering team
      done: true
