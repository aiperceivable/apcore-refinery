# Refined by apcore-refinery
# Original: Auto-generated by flask-apcore scanner (2026-03-15)
# Refined: 2026-03-17

bindings:
- module_id: delete_task.delete
  target: app:delete_task
  description: Permanently delete a task by its ID (destructive, irreversible).
  documentation: >
    Permanently removes a task from the in-memory task store by its integer ID.
    This operation is irreversible — once deleted, the task cannot be recovered.
    Returns {"deleted": true} on success. Returns a 404 error if no task exists
    with the given ID. Does not affect other tasks or the ID counter — deleted
    IDs are not reused.
  tags:
  - tasks
  - delete
  - destructive
  version: 1.0.0
  annotations:
    readonly: false
    destructive: true
    idempotent: false
    requires_approval: true
    open_world: false
    streaming: false
    cacheable: false
    cache_ttl: 0
    cache_key_fields: null
    paginated: false
    pagination_style: cursor
  metadata:
    source: native
    x-when-to-use: >
      When the user explicitly wants to remove a task they no longer need. Always
      confirm the task ID with the user before calling — deletion is permanent.
    x-when-not-to-use: >
      Do not use to mark a task as complete — use update_task.put with done=true instead.
      Do not use to clear all tasks — this only deletes one task at a time.
    x-common-mistakes: >
      Deleting a task without first confirming the ID with the user. Passing a task
      title instead of the integer task_id. Assuming the operation is idempotent —
      calling delete on an already-deleted ID returns a 404 error.
    x-workflow-hints: >
      Typical flow: list_tasks.get to find the task, get_task.get to confirm it is
      the right one, then delete_task.delete to remove it.
  input_schema:
    type: object
    properties:
      task_id:
        type: integer
        description: The unique integer identifier of the task to delete.
        x-llm-description: "Required. The integer ID of the task to delete. Use list_tasks.get to discover valid IDs."
    required:
    - task_id
  output_schema:
    type: object
  examples:
  - title: "Delete a completed task by ID 1"
    inputs:
      task_id: 1
    output:
      deleted: true
  - title: "Attempt to delete a task that does not exist"
    inputs:
      task_id: 999
    output:
      error: "not found"
