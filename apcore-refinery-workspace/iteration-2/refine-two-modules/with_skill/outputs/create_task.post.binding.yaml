# Refined by apcore-refinery
# Original: Auto-generated by flask-apcore scanner (2026-03-15)
# Refined: 2026-03-17

bindings:
- module_id: create_task.post
  target: app:create_task
  description: Create a new task in the task manager with a title, optional description, and completion status.
  documentation: >
    Creates a new task and stores it in the in-memory task store. The task is assigned
    an auto-incrementing integer ID. Only the title field is required; description
    defaults to an empty string and done defaults to false. Returns the full Task
    object including the server-assigned ID. This is a write operation that mutates
    server state on every call.
  tags:
  - tasks
  - create
  - write
  version: 1.0.0
  annotations:
    readonly: false
    destructive: false
    idempotent: false
    requires_approval: false
    open_world: false
    streaming: false
    cacheable: false
    cache_ttl: 0
    cache_key_fields: null
    paginated: false
    pagination_style: cursor
  metadata:
    source: native
    x-when-to-use: >
      When the user wants to add a new task to the task list. Requires at least a title.
      Use this for new tasks only — to modify an existing task, use update_task.put instead.
    x-when-not-to-use: >
      Do not use to update an existing task — use update_task.put instead.
      Do not use to mark a task as done — use update_task.put with done=true.
    x-common-mistakes: >
      Passing an ID field — IDs are auto-assigned by the server and must not be provided.
      Omitting the required title field. Sending done=true at creation when the intent
      is to create a pending task.
    x-workflow-hints: >
      Typical flow: list_tasks.get to see current tasks, create_task.post to add a new one,
      then list_tasks.get or get_task.get to confirm creation.
  input_schema:
    type: object
    properties:
      title:
        title: Title
        type: string
        description: The name or short summary of the task to create.
        x-llm-description: "Required. The task title — a short descriptive name like 'Buy groceries' or 'Review PR #42'."
      description:
        default: ''
        title: Description
        type: string
        description: An optional longer description providing details about the task.
        x-llm-description: "Optional. Additional details about the task. Defaults to empty string if omitted."
      done:
        default: false
        title: Done
        type: boolean
        description: Whether the task is already completed at creation time.
        x-llm-description: "Optional. Set to true to create a task already marked as complete. Defaults to false."
    required:
    - title
  output_schema:
    properties:
      id:
        title: Id
        type: integer
      title:
        title: Title
        type: string
      description:
        title: Description
        type: string
      done:
        title: Done
        type: boolean
    required:
    - id
    - title
    - description
    - done
    title: Task
    type: object
  examples:
  - title: "Create a pending task with title and description"
    inputs:
      title: "Review pull request"
      description: "Review the flask-apcore integration PR and leave comments"
      done: false
    output:
      id: 3
      title: "Review pull request"
      description: "Review the flask-apcore integration PR and leave comments"
      done: false
  - title: "Create a minimal task with only a title"
    inputs:
      title: "Set up CI pipeline"
    output:
      id: 3
      title: "Set up CI pipeline"
      description: ""
      done: false
